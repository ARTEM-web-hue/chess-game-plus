{% extends "base.html" %}

{% block content %}
<div class="dashboard">
    <h2>Добро пожаловать, {{ user.username }}! 👋</h2>
    
    <div class="stats-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1rem; margin: 2rem 0;">
        <div style="background: rgba(46, 204, 113, 0.2); padding: 1.5rem; border-radius: 15px; text-align: center;">
            <h3 style="color: #2ecc71;">🏆 Победы</h3>
            <p style="font-size: 2rem; font-weight: bold;">{{ stats.wins }}</p>
        </div>
        
        <div style="background: rgba(231, 76, 60, 0.2); padding: 1.5rem; border-radius: 15px; text-align: center;">
            <h3 style="color: #e74c3c;">💔 Поражения</h3>
            <p style="font-size: 2rem; font-weight: bold;">{{ stats.losses }}</p>
        </div>
        
        <div style="background: rgba(52, 152, 219, 0.2); padding: 1.5rem; border-radius: 15px; text-align: center;">
            <h3 style="color: #3498db;">🤝 Ничьи</h3>
            <p style="font-size: 2rem; font-weight: bold;">{{ stats.draws }}</p>
        </div>
        
        <div style="background: rgba(243, 156, 18, 0.2); padding: 1.5rem; border-radius: 15px; text-align: center;">
            <h3 style="color: #f39c12;">⭐ Рейтинг</h3>
            <p style="font-size: 2rem; font-weight: bold;">{{ stats.rating }}</p>
        </div>
    </div>

    <div style="display: flex; gap: 1rem; justify-content: center; margin: 2rem 0;">
        <a href="{{ url_for('play') }}" class="btn">
            <i class="fas fa-chess-board"></i> Начать Игру
        </a>
    </div>

    <div style="background: rgba(255,255,255,0.1); padding: 2rem; border-radius: 15px; margin: 2rem 0;">
        <h3>🎯 Достижения</h3>
        {% if achievements %}
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1rem; margin-top: 1rem;">
                {% for achievement in achievements %}
                    <div style="background: rgba(255,255,255,0.05); padding: 1rem; border-radius: 10px;">
                        <strong>{{ achievement.name }}</strong>
                        <p style="margin: 0.5rem 0 0 0; font-size: 0.9rem; opacity: 0.8;">
                            {{ achievement.description }}
                        </p>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <p style="text-align: center; opacity: 0.7; margin: 2rem 0;">
                Пока нет достижений. Начните играть, чтобы их получить!
            </p>
        {% endif %}
    </div>
</div>
{% endblock %}
